name: Validate images

on:
  workflow_call:
    inputs:
      ARCH:
        required: true
        type: string
      # a tag from https://github.com/NVIDIA/container-canary/releases
      CONTAINER_CANARY_VERSION:
        description: 'tag from https://github.com/NVIDIA/container-canary/releases'
        required: true
        type: string
      CUDA_VER:
        required: true
        type: string
      DRIVER:
        required: true
        type: string
      GPU:
        required: true
        type: string
      PYTHON_VER:
        required: true
        type: string
      BASE_TAG:
        required: true
        type: string
      NOTEBOOKS_TAG:
        required: true
        type: string

defaults:
  run:
    shell: sh

permissions:
  actions: read
  checks: none
  contents: read
  deployments: none
  discussions: none
  id-token: write
  issues: none
  packages: read
  pages: none
  pull-requests: read
  repository-projects: none
  security-events: none
  statuses: none

jobs:
  validate:
    strategy:
      matrix:
        ARCH: ["${{ inputs.ARCH }}"]
        CUDA_VER: ["${{ inputs.CUDA_VER }}"]
        PYTHON_VER: ["${{ inputs.PYTHON_VER }}"]
        GPU: ["${{ inputs.GPU }}"]
        DRIVER: ["${{ inputs.DRIVER }}"]
        IMAGE_REPO: ["base", "notebooks"]
      fail-fast: false
    runs-on: "linux-${{ inputs.ARCH }}-gpu-${{ inputs.GPU }}-${{ inputs.DRIVER }}-1"
    container:
      image: 'docker:dind'
      options: --privileged
      env:
        NVIDIA_VISIBLE_DEVICES: ${{ env.NVIDIA_VISIBLE_DEVICES }}
    steps:
      - name: Install required tools
        run: |
          apk add curl
      - name: Install container-canary
        run: |
          BINARY_URI=https://github.com/NVIDIA/container-canary/releases/download/${{ inputs.CONTAINER_CANARY_VERSION}}/canary_linux_${{ inputs.ARCH }}
          curl --retry 3 --retry-delay 10 -sSL ${BINARY_URI} > /usr/local/bin/canary
          chmod +x /usr/local/bin/canary
          canary version
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Print environment
        run: |
          nvidia-smi
      - name: (base) container-canary checks
        run: |
          ./ci/container-canary/run-checks.sh ${{ inputs.BASE_TAG }}
      - name: (notebooks) container-canary checks
        run: |
          ./ci/container-canary/run-checks.sh ${{ inputs.NOTEBOOK_TAG }}
