{# This partial is used to display debug information about the current conda env #}

{# Output current conda env info #}
RUN source activate rapids \
  && env \
  && conda info \
  && conda config --show-sources \
  && conda list --show-channel-urls
