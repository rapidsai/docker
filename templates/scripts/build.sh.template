#!/bin/bash
set -e

# Assume this scrip resides in the "rapids" dir which contains the
# cloned repos.

WORK_DIR=`pwd`

#:# Insert the contents of the build script in utils for each RAPIDS component
#:# listed in the config
runcommand ../../commands/utils/dumpRapidsBuildScriptsFromConfig.sh
